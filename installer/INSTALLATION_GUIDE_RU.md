# Руководство по установке Forester

## 🎯 Для конечных пользователей

### Шаг 1: Подготовка

1. Скачайте архив с установщиком
2. Распакуйте архив в любую папку
3. Откройте терминал/командную строку в папке `installer`

### Шаг 2: Запуск установщика

#### Linux
```bash
./install.sh
```

#### macOS
```bash
./install.sh
```

#### Windows
```cmd
install.bat
```
*Или дважды кликните на файл `install.bat`*

### Шаг 3: Следуйте инструкциям

Установщик попросит:
1. **Выбрать путь установки** (можно оставить по умолчанию)
2. **Установить аддоны** (выберите нужные редакторы)

### Шаг 4: Проверка

После установки проверьте работу:

```bash
forester --version
```

Если команда не найдена, добавьте путь в PATH (см. ниже).

## 🔧 Настройка PATH

### Linux

**Временное добавление:**
```bash
export PATH="/opt/Forester/bin:$PATH"
```

**Постоянное добавление:**
```bash
echo 'export PATH="/opt/Forester/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

**Или через симлинк:**
```bash
sudo ln -s /opt/Forester/bin/forester /usr/local/bin/forester
```

### macOS

**Добавьте в ~/.zshrc:**
```bash
echo 'export PATH="/Applications/Forester/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

**Или через симлинк:**
```bash
sudo ln -s /Applications/Forester/bin/forester /usr/local/bin/forester
```

### Windows

**Через графический интерфейс:**
1. Откройте "Система" → "Дополнительные параметры системы"
2. Нажмите "Переменные среды"
3. В "Системные переменные" найдите `Path`
4. Нажмите "Изменить" → "Создать"
5. Добавьте: `C:\Program Files\Forester\bin`
6. Нажмите "ОК" во всех окнах

**Через командную строку:**
```cmd
setx PATH "%PATH%;C:\Program Files\Forester\bin"
```
*Требует перезапуска командной строки*

## 📦 Установка аддонов

### Blender

Аддон устанавливается автоматически, если вы выбрали его при установке.

**Ручная установка:**

1. Найдите папку аддонов Blender:
   - **Linux:** `~/.config/blender/{версия}/scripts/addons/`
   - **macOS:** `~/Library/Application Support/Blender/{версия}/scripts/addons/`
   - **Windows:** `%APPDATA%\Blender Foundation\Blender\{версия}\scripts\addons\`

2. Скопируйте папку `difference-machine` из `installer/addons/blender/` в папку аддонов

3. В Blender:
   - Откройте `Edit` → `Preferences` → `Add-ons`
   - Найдите "Difference Machine"
   - Включите аддон

## 🛠️ Для разработчиков

### Сборка установщика

1. **Соберите бинарники Go:**
   ```bash
   cd forester
   
   # Для текущей платформы:
   make build
   
   # Или для всех платформ (кросс-компиляция):
   make cross-build
   
   # Альтернативно используйте платформо-специфичные скрипты:
   ./LINUX_build_and_install.sh    # Linux
   ./MACOS_build_and_install.sh    # macOS
   ```

2. **Соберите установщик:**
   ```bash
   cd installer
   ./build_installer.sh
   ```

3. **Проверьте результат:**
   ```bash
   ls installer/forester/linux/bin/forester
   ls installer/forester/windows/bin/forester.exe
   ls installer/forester/macos/bin/forester
   ```

### Упаковка для распространения

```bash
cd installer
VERSION="1.0.0"

# TAR.GZ (Linux/macOS)
tar -czf "../forester-installer-v${VERSION}.tar.gz" \
    forester/ addons/ \
    install.sh install.bat README.txt README.md INSTALLATION_GUIDE_RU.md

# ZIP (Windows)
zip -r "../forester-installer-v${VERSION}.zip" \
    forester/ addons/ \
    install.sh install.bat README.txt README.md INSTALLATION_GUIDE_RU.md
```

## ❓ Решение проблем

### Проблема: "Бинарник не найден"

**Решение:**
- Убедитесь, что бинарники Go собраны для вашей ОС (`cd forester && make build`)
- Проверьте наличие файлов в `installer/{os}/bin/`
- Запустите `build_installer.sh` для копирования бинарников
- Убедитесь, что Go 1.21+ установлен и сборка прошла успешно

### Проблема: "Нет прав на запись"

**Решение:**
- **Linux/macOS:** Используйте `sudo` или выберите пользовательскую директорию
- **Windows:** Запустите командную строку от имени администратора

### Проблема: "Команда forester не найдена"

**Решение:**
- Добавьте путь в PATH (см. раздел "Настройка PATH")
- Или используйте полный путь: `/opt/Forester/bin/forester`

### Проблема: "Аддон не найден в Blender"

**Решение:**
1. Проверьте, что аддон скопирован в правильную папку
2. Убедитесь, что версия Blender соответствует требованиям (4.5.0+)
3. Перезапустите Blender
4. Проверьте, что аддон включен в настройках

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Изучите логи установки
3. Создайте issue в репозитории проекта

---

**Удачной установки! 🚀**
