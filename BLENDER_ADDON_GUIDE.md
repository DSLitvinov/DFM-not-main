# Руководство по аддону Difference Machine для Blender

## 📋 Описание

Difference Machine - это аддон для Blender, который предоставляет графический интерфейс для работы с системой контроля версий Forester. Аддон позволяет управлять версиями 3D-проектов прямо из Blender без необходимости использования командной строки.

## 🎯 Возможности

- ✅ Инициализация репозиториев Forester
- ✅ Создание и просмотр коммитов
- ✅ Работа с ветками (создание, переключение, удаление)
- ✅ Просмотр истории коммитов
- ✅ Сравнение версий
- ✅ Работа с тегами
- ✅ Управление Stash (временное сохранение изменений)
- ✅ Garbage Collection с настройками
- ✅ Система review для 3D-моделей
- ✅ Автоматическое планирование GC
- ✅ Блокировка файлов

## 📦 Требования

### Обязательные

- **Blender 4.5.0** или выше
- **Forester CLI** - установленный и настроенный
  - Linux: `/opt/Forester/bin/forester`
  - macOS: `/Applications/Forester/bin/forester`
  - Windows: `C:\Program Files\Forester\bin\forester.exe`

### Рекомендуемые

- Python 3.10+ (встроен в Blender)
- Достаточно места на диске для хранения версий

## 🚀 Установка

### Способ 1: Через установщик (рекомендуется)

```bash
cd installer
./install.sh    # Linux/macOS
# или
install.bat     # Windows
```

Установщик автоматически:
- Установит Forester CLI
- Установит аддон в Blender
- Настроит конфигурацию

### Способ 2: Ручная установка

#### Шаг 1: Установка Forester CLI

См. [forester/BUILD.md](forester/BUILD.md) или [forester/GUIDE.md](forester/GUIDE.md) для инструкций по сборке.

#### Шаг 2: Настройка конфигурации

**Linux/macOS:**
```bash
mkdir -p ~/.dfm-setup
cat > ~/.dfm-setup/setup.cfg << 'CFG'
[forester]
path = /opt/Forester
CFG
```

**Windows:**
Скрипт сборки копирует бинарник в `installer/forester/windows/bin/forester.exe`. Создайте конфигурационный файл:

```cmd
mkdir %USERPROFILE%\.dfm-setup
echo [forester] > %USERPROFILE%\.dfm-setup\setup.cfg
echo path = D:\Git\difference-machine\installer\forester\windows\bin\forester.exe >> %USERPROFILE%\.dfm-setup\setup.cfg
```

Или через PowerShell:
```powershell
$configDir = "$env:USERPROFILE\.dfm-setup"
New-Item -ItemType Directory -Path $configDir -Force
@"
[forester]
path = D:\Git\difference-machine\installer\forester\windows\bin\forester.exe
"@ | Out-File -FilePath "$configDir\setup.cfg" -Encoding UTF8
```

#### Шаг 3: Установка аддона в Blender

1. **Найдите директорию аддонов Blender:**
   - **Linux:** `~/.config/blender/{version}/scripts/addons/`
   - **macOS:** `~/Library/Application Support/Blender/{version}/scripts/addons/`
   - **Windows:** `%APPDATA%\Blender Foundation\Blender\{version}\scripts\addons\`

2. **Скопируйте аддон:**
   ```bash
   # Linux/macOS
   cp -r addons/blender/difference-machine ~/.config/blender/4.5/scripts/addons/
   
   # Windows (PowerShell)
   Copy-Item -Recurse "addons\blender\difference-machine" "$env:APPDATA\Blender Foundation\Blender\4.5\scripts\addons\"
   ```

3. **Включите аддон в Blender:**
   - Откройте Blender
   - Перейдите в `Edit` → `Preferences` → `Add-ons`
   - Найдите "Difference Machine"
   - Включите аддон

## 🎮 Использование

### Первый запуск

1. **Откройте Blender** с вашим проектом
2. **Найдите панель Difference Machine** в боковой панели (нажмите `N` в 3D Viewport)
3. **Инициализируйте репозиторий:**
   - Нажмите кнопку "Initialize Repository"
   - Или используйте оператор через поиск (`F3` → "Initialize Repository")

### Основные операции

#### Создание коммита

1. Внесите изменения в ваш проект
2. В панели Difference Machine нажмите "Commit"
3. Введите сообщение коммита
4. (Опционально) Укажите автора
5. Нажмите "Create Commit"

#### Просмотр истории

1. В панели Difference Machine нажмите "History"
2. Выберите коммит из списка
3. Просмотрите детали коммита
4. Используйте кнопки для навигации

#### Работа с ветками

1. **Создание ветки:**
   - Нажмите "Create Branch"
   - Введите имя ветки
   - Нажмите "Create"

2. **Переключение ветки:**
   - Выберите ветку из списка
   - Нажмите "Checkout Branch"

3. **Удаление ветки:**
   - Выберите ветку
   - Нажмите "Delete Branch"

#### Работа со Stash

**Что такое Stash?**

Stash (временное хранилище) позволяет сохранить незакоммиченные изменения в рабочей директории без создания коммита. Это полезно, когда нужно переключиться на другую ветку или коммит, но сохранить текущие изменения.

**Автоматическое создание Stash:**

При попытке переключиться на другой коммит или ветку с незакоммиченными изменениями:
1. Появится диалог: "There are uncommitted changes, please commit or stash"
2. Нажмите кнопку **"Stash"** для автоматического сохранения изменений
3. Stash будет создан с автоматическим сообщением и timestamp
4. После этого checkout будет выполнен автоматически

**Ручное управление Stash:**

1. **Откройте панель "Stash Management"** в боковой панели Difference Machine

2. **Создание Stash:**
   - Нажмите кнопку **"Save Stash"**
   - Введите описание stash (например, "Work in progress on model")
   - Stash будет сохранен со всеми текущими изменениями

3. **Просмотр Stash:**
   - В панели отображается список всех stash
   - Каждый stash показывает короткий хеш и сообщение
   - Нажмите **"Refresh Stashes"** для обновления списка

4. **Применение Stash:**
   - **Apply** - применяет изменения из stash к текущей рабочей директории, но оставляет stash в списке
   - **Pop** - применяет изменения и удаляет stash из списка

5. **Удаление Stash:**
   - **Drop** - удаляет stash без применения изменений

**Примеры использования:**

- **Переключение ветки с незакоммиченными изменениями:**
  ```
  Switch Branch → Диалог "Uncommitted changes" → Stash → Переключение выполнено
  Позже: Stash Management → Выберите stash → Pop (восстановить изменения)
  ```

- **Сохранение промежуточного состояния:**
  ```
  Stash Management → Save Stash → "Experiment variant A" → OK
  Позже: Apply или Pop stash для восстановления состояния
  ```

**Важные замечания:**

- ⚠️ Stash сохраняет только файлы проекта, не состояние Blender (открытые объекты, настройки сцены)
- ⚠️ Stash не сохраняет игнорируемые файлы (указанные в `.dfmignore`)
- ✅ Stash можно применять многократно (используйте "Apply")
- ✅ Список stash автоматически обновляется после операций

#### Просмотр статуса

Панель Difference Machine автоматически показывает:
- Текущую ветку
- Количество измененных файлов
- Статус репозитория

### Продвинутые функции

#### Garbage Collection

1. **Ручной запуск:**
   - Перейдите в `Edit` → `Preferences` → `Add-ons` → `Difference Machine`
   - В разделе "Garbage Collection" нажмите "Garbage Collect Now"

2. **Автоматический запуск:**
   - В настройках аддона включите "Enable Scheduled GC"
   - Установите время и интервал запуска
   - GC будет запускаться автоматически

3. **Настройка срока хранения reflog:**
   - В настройках установите "Reflog Expiration (Days)"
   - По умолчанию: 90 дней

#### Система Review

1. **Добавление комментария:**
   - Выберите объект в сцене
   - Используйте оператор "Review Comment"
   - Введите комментарий

2. **Просмотр комментариев:**
   - Используйте оператор "List Reviews"
   - Выберите ресурс для просмотра комментариев

3. **Одобрение/отклонение:**
   - Используйте операторы "Approve" или "Reject"

#### Блокировка файлов

1. **Блокировка файла:**
   - Используйте оператор "Lock File"
   - Выберите файл для блокировки
   - Выберите тип блокировки (exclusive/shared)

2. **Просмотр блокировок:**
   - Используйте оператор "List Locks"

3. **Разблокировка:**
   - Используйте оператор "Unlock File"

## ⚙️ Настройки аддона

Доступны через `Edit` → `Preferences` → `Add-ons` → `Difference Machine`:

### Commit Settings

- **Default Author** - имя автора по умолчанию для коммитов

### Garbage Collection

- **Reflog Expiration (Days)** - срок хранения reflog (по умолчанию 90 дней)
- **Enable Scheduled GC** - включить автоматический GC
- **Hour** - час запуска GC (0-23)
- **Minute** - минута запуска GC (0-59)
- **Interval (Days)** - интервал между запусками GC

## 🔍 Поиск операторов

Все операторы доступны через поиск Blender:

1. Нажмите `F3` (или `Space` в некоторых режимах)
2. Введите название операции (например, "commit", "branch", "history")
3. Выберите нужный оператор

### Список основных операторов

- `DF_OT_initialize_repository` - Инициализация репозитория
- `DF_OT_create_commit` - Создание коммита
- `DF_OT_show_history` - Просмотр истории
- `DF_OT_create_branch` - Создание ветки
- `DF_OT_checkout_branch` - Переключение ветки
- `DF_OT_delete_branch` - Удаление ветки
- `DF_OT_save_stash` - Сохранение stash
- `DF_OT_apply_stash` - Применение stash
- `DF_OT_pop_stash` - Применение и удаление stash
- `DF_OT_stash_drop` - Удаление stash
- `DF_OT_refresh_stashes` - Обновление списка stash
- `DF_OT_garbage_collect` - Garbage Collection
- `DF_OT_show_status` - Просмотр статуса

## 🐛 Решение проблем

### Аддон не находит Forester CLI

1. **Проверьте установку Forester:**
   ```bash
   # Linux/macOS
   /opt/Forester/bin/forester --version
   
   # Windows
   & "C:\Program Files\Forester\bin\forester.exe" --version
   ```

2. **Проверьте конфигурационный файл:**
   ```bash
   # Linux/macOS
   cat ~/.dfm-setup/setup.cfg
   
   # Windows
   Get-Content "$env:USERPROFILE\.dfm-setup\setup.cfg"
   ```

3. **Проверьте логи:**
   - Логи аддона: `~/.blender_addon.log` (Linux/macOS) или `%USERPROFILE%\.blender_addon.log` (Windows)
   - Консоль Blender: `Window` → `Toggle System Console`

### Ошибки при создании коммита

1. **Убедитесь, что репозиторий инициализирован:**
   - Используйте оператор "Initialize Repository"

2. **Проверьте права доступа:**
   - Убедитесь, что у Blender есть права на запись в директорию проекта

3. **Проверьте наличие изменений:**
   - Используйте "Status" для просмотра измененных файлов

### Аддон не отображается в списке

1. **Проверьте установку:**
   - Убедитесь, что аддон скопирован в правильную директорию
   - Проверьте версию Blender (требуется 4.5.0+)

2. **Проверьте файл `__init__.py`:**
   - Убедитесь, что файл существует и корректен

3. **Перезагрузите аддоны:**
   - В настройках аддонов нажмите "Refresh"

## 📝 Примеры использования

### Базовый workflow

1. **Инициализация проекта:**
   ```
   Откройте Blender → Панель Difference Machine → Initialize Repository
   ```

2. **Создание первого коммита:**
   ```
   Создайте модель → Commit → "Initial commit"
   ```

3. **Работа с ветками:**
   ```
   Create Branch → "feature-new-model" → Checkout Branch
   ```

4. **Создание коммита в ветке:**
   ```
   Внесите изменения → Commit → "Add new model"
   ```

5. **Возврат в main:**
   ```
   Checkout Branch → "main"
   ```

### Работа со Stash

1. **Сохранение изменений перед checkout:**
   ```
   Checkout → Диалог "Uncommitted changes" → Stash → Checkout выполнен
   ```

2. **Восстановление stash:**
   ```
   Stash Management → Выберите stash → Pop (применить и удалить)
   ```

3. **Создание именованного stash:**
   ```
   Stash Management → Save Stash → "Work in progress" → OK
   ```

### Работа с тегами

1. **Создание тега:**
   ```
   History → Выберите коммит → Create Tag → "v1.0.0"
   ```

2. **Просмотр тегов:**
   ```
   History → Просмотр коммитов с тегами
   ```

### Очистка репозитория

1. **Предпросмотр GC:**
   ```
   Preferences → Difference Machine → Garbage Collection → Dry Run
   ```

2. **Запуск GC:**
   ```
   Preferences → Difference Machine → Garbage Collection → Garbage Collect Now
   ```

## 🔗 Полезные ссылки

- [Документация Forester CLI](forester/README.md)
- [Полное руководство по Forester](forester/GUIDE.md)
- [Инструкция по сборке](forester/BUILD.md)
- [Python API документация](forester_api/README.md)

## 📄 Структура аддона

```
difference-machine/
├── __init__.py              # Точка входа аддона
├── blender_manifest.toml    # Манифест для Blender
├── preferences.py           # Настройки аддона
├── operators/               # Операторы (команды)
│   ├── init_operators.py   # Инициализация
│   ├── commit_operators.py # Коммиты
│   ├── branch_operators.py # Ветки
│   ├── history_operators.py # История
│   ├── stash_operators.py  # Stash операции
│   ├── gc_operators.py     # Garbage Collection
│   └── review_operators.py  # Система review
├── properties/              # Свойства (данные)
│   ├── properties.py        # Основные свойства
│   └── review_properties.py # Свойства review
├── ui/                      # Интерфейс
│   ├── ui_panels.py        # Панели UI
│   ├── ui_lists.py         # Списки
│   └── ui_main.py          # Главный UI
└── utils/                   # Утилиты
    ├── forester_cli.py     # Обертка для CLI
    ├── config_loader.py    # Загрузка конфигурации
    └── helpers.py          # Вспомогательные функции
```

## ⚠️ Важные замечания

1. **Аддон использует ТОЛЬКО CLI** - Python bindings не используются
2. **Все операции выполняются через Forester CLI** - убедитесь, что CLI установлен и настроен
3. **Рекомендуется регулярно запускать GC** - для очистки удаленных коммитов
4. **Блокировка файлов** - используйте для предотвращения конфликтов при совместной работе
5. **Система review** - полезна для командной работы над проектами

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи аддона
2. Проверьте консоль Blender
3. Убедитесь, что Forester CLI установлен и работает
4. Создайте issue в репозитории проекта с описанием проблемы

---

**Версия аддона:** 1.0.0  
**Требуемая версия Blender:** 4.5.0+  
**Последнее обновление:** 2024





