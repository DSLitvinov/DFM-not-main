# Forester Installer - Инструкция по использованию

## 📦 Описание

Этот установщик позволяет легко установить Forester CLI (написанный на Go) и аддоны для различных редакторов на всех поддерживаемых операционных системах.

## 📁 Структура установщика

```
installer/
├── forester/
│   ├── linux/
│   │   └── bin/
│   │       └── forester          # Бинарник для Linux
│   ├── windows/
│   │   └── bin/
│   │       └── forester.exe     # Бинарник для Windows
│   └── macos/
│       └── bin/
│           └── forester          # Бинарник для macOS
├── addons/
│   └── blender/
│       └── difference-machine/  # Аддон для Blender
├── install.sh               # Скрипт установки (Linux/macOS)
├── install.bat              # Скрипт установки (Windows)
└── build_installer.sh       # Скрипт для сборки установщика
```

## 🚀 Быстрый старт

### Для пользователей

1. **Скачайте установщик** (архив с папкой `installer/`)
2. **Распакуйте архив**
3. **Запустите установщик:**
   - **Linux/macOS:** `./install.sh`
   - **Windows:** `install.bat`

### Для разработчиков

#### Сборка установщика

1. **Соберите бинарники Go** для нужных платформ:
   ```bash
   cd forester
   # Для текущей платформы:
   make build
   # Или для всех платформ (кросс-компиляция):
   make cross-build
   ```
   
   Альтернативно можно использовать скрипты сборки:
   ```bash
   cd forester
   ./LINUX_build_and_install.sh    # Linux
   ./MACOS_build_and_install.sh    # macOS
   WINDOWS_build_and_install.bat   # Windows
   ```
   
   Эти скрипты автоматически копируют бинарники в `installer/forester/{os}/bin/`.

2. **Соберите установщик:**
   ```bash
   cd installer
   ./build_installer.sh
   ```

   Скрипт автоматически:
   - Скопирует бинарники Go из `forester/build/` или `forester/bin/` в `installer/forester/{os}/bin/`
   - Скопирует аддоны из `addons/` в `installer/addons/`
   - Создаст README.txt
   
   **Примечание:** Скрипты сборки (`LINUX_build_and_install.sh`, `MACOS_build_and_install.sh`, `WINDOWS_build_and_install.bat`) автоматически копируют бинарники в `installer/forester/{os}/bin/`, поэтому шаг 2 может быть пропущен, если использовались эти скрипты.

3. **Упакуйте для распространения:**
   ```bash
   cd installer
   tar -czf forester-installer-v1.0.0.tar.gz \
       linux/ windows/ macos/ addons/ \
       install.sh install.bat README.txt README.md
   ```

## 📋 Подробная инструкция

### Установка на Linux

1. **Запустите установщик:**
   ```bash
   cd installer
   ./install.sh
   ```

2. **Выберите путь установки:**
   - По умолчанию: `/opt/Forester`
   - Можно указать свой путь (например, `~/Forester`)

3. **Установка аддонов:**
   - Установщик автоматически найдет установленные версии Blender
   - Выберите, для каких версий установить аддон
   - Или укажите путь вручную

4. **Добавление в PATH (опционально):**
   ```bash
   # Временное добавление (для текущей сессии)
   export PATH="/opt/Forester/bin:$PATH"
   
   # Постоянное добавление (добавьте в ~/.bashrc или ~/.zshrc)
   echo 'export PATH="/opt/Forester/bin:$PATH"' >> ~/.bashrc
   source ~/.bashrc
   
   # Или создайте симлинк
   sudo ln -s /opt/Forester/bin/forester /usr/local/bin/forester
   ```

5. **Проверка установки:**
   ```bash
   forester --version
   # или
   /opt/Forester/bin/forester --version
   ```

### Установка на macOS

1. **Запустите установщик:**
   ```bash
   cd installer
   ./install.sh
   ```

2. **Выберите путь установки:**
   - По умолчанию: `/Applications/Forester`
   - Можно указать свой путь

3. **Установка аддонов:**
   - Установщик найдет Blender в `~/Library/Application Support/Blender/`
   - Выберите версии для установки

4. **Добавление в PATH (опционально):**
   ```bash
   # Добавьте в ~/.zshrc или ~/.bash_profile
   echo 'export PATH="/Applications/Forester/bin:$PATH"' >> ~/.zshrc
   source ~/.zshrc
   
   # Или создайте симлинк
   sudo ln -s /Applications/Forester/bin/forester /usr/local/bin/forester
   ```

5. **Проверка установки:**
   ```bash
   forester --version
   ```

### Установка на Windows

1. **Запустите установщик:**
   - Дважды кликните на `install.bat`
   - Или откройте командную строку и выполните:
     ```cmd
     cd installer
     install.bat
     ```

2. **Выберите путь установки:**
   - По умолчанию: `C:\Program Files\Forester`
   - Можно указать свой путь

3. **Права администратора:**
   - Для установки в `C:\Program Files\` могут потребоваться права администратора
   - Запустите командную строку от имени администратора

4. **Установка аддонов:**
   - Установщик найдет Blender в `%APPDATA%\Blender Foundation\Blender\`
   - Выберите версии для установки

5. **Добавление в PATH (опционально):**
   ```cmd
   # Через графический интерфейс:
   # 1. Откройте "Система" → "Дополнительные параметры системы"
   # 2. Нажмите "Переменные среды"
   # 3. Добавьте C:\Program Files\Forester\bin в PATH
   
   # Или через командную строку (требует перезапуска):
   setx PATH "%PATH%;C:\Program Files\Forester\bin"
   ```

6. **Проверка установки:**
   ```cmd
   forester --version
   ```

## 🔧 Настройка для аддона Blender

После установки аддон автоматически появится в Blender. Для настройки:

1. **Создайте конфигурационный файл** (если нужно указать путь вручную):
   ```bash
   # Linux/macOS
   mkdir -p ~/.dfm-setup
   cat > ~/.dfm-setup/setup.cfg << 'EOF'
   [forester]
   path = /opt/Forester
   EOF
   ```

   ```cmd
   REM Windows
   mkdir %USERPROFILE%\.dfm-setup
   echo [forester] > %USERPROFILE%\.dfm-setup\setup.cfg
   echo path = C:\Program Files\Forester >> %USERPROFILE%\.dfm-setup\setup.cfg
   ```

2. **В Blender:**
   - Откройте `Edit` → `Preferences` → `Add-ons`
   - Найдите "Difference Machine"
   - Включите аддон
   - Аддон автоматически найдет бинарник Forester

## 🛠️ Сборка установщика для разработчиков

### Требования

- Собранные бинарники для нужных платформ
- Bash (для Linux/macOS)
- Доступ к исходникам проекта

### Процесс сборки

1. **Соберите бинарники Go:**
   ```bash
   cd forester
   
   # Для текущей платформы:
   make build
   
   # Для всех платформ (кросс-компиляция):
   make cross-build
   
   # Или используйте платформо-специфичные скрипты:
   ./LINUX_build_and_install.sh    # Linux
   ./MACOS_build_and_install.sh    # macOS
   ```
   
   Бинарники будут находиться в `forester/build/` (или `forester/bin/` при использовании скриптов).

2. **Запустите скрипт сборки установщика:**
   ```bash
   cd installer
   ./build_installer.sh
   ```

3. **Проверьте результат:**
   ```bash
   ls -la installer/forester/linux/bin/
   ls -la installer/forester/windows/bin/
   ls -la installer/forester/macos/bin/
   ls -la installer/addons/
   ```

4. **Упакуйте для распространения:**
   ```bash
   cd installer
   VERSION="1.0.0"
   tar -czf "../forester-installer-v${VERSION}.tar.gz" \
       linux/ windows/ macos/ addons/ \
       install.sh install.bat README.txt README.md
   
   # Или ZIP для Windows
   zip -r "../forester-installer-v${VERSION}.zip" \
       linux/ windows/ macos/ addons/ \
       install.sh install.bat README.txt README.md
   ```

### Автоматизация через CI/CD

Пример для GitHub Actions:

```yaml
name: Build Installer

on:
  release:
    types: [created]

jobs:
  build-installer:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'
      
      - name: Build Forester
        run: |
          cd forester
          make cross-build
      
      - name: Build Installer
        run: |
          cd installer
          ./build_installer.sh
      
      - name: Upload Installer
        uses: actions/upload-artifact@v3
        with:
          name: installer-${{ matrix.os }}
          path: installer/
```

## ❓ Часто задаваемые вопросы

### Q: Бинарник не найден при установке

**A:** Убедитесь, что:
1. Бинарники Go собраны для вашей ОС (`cd forester && make build` или `make cross-build`)
2. Запущен `build_installer.sh` для копирования бинарников
3. Бинарник находится в правильной папке (`installer/forester/{os}/bin/`)
4. Проверьте наличие Go 1.21+ и что сборка прошла успешно

### Q: Нужны ли права администратора?

**A:** 
- **Linux/macOS:** Только для установки в системные директории (`/opt`, `/usr`, `/Applications`)
- **Windows:** Для установки в `C:\Program Files\`
- Для пользовательских директорий права не требуются

### Q: Как обновить установку?

**A:** 
1. Запустите установщик заново
2. Укажите тот же путь установки
3. Бинарники будут перезаписаны

### Q: Можно ли установить только аддоны без CLI?

**A:** Да, можно вручную скопировать аддоны из `installer/addons/` в нужные директории редакторов.

### Q: Где хранятся бинарники в git?

**A:** Бинарники **не хранятся** в git. Они исключены через `.gitignore`. В репозитории только структура папок и скрипты.

## 📝 Структура после установки

После установки структура будет следующей:

```
/opt/Forester/              # Linux
├── bin/
│   └── forester

~/.config/blender/4.0/scripts/addons/  # Linux
└── difference-machine/

/Applications/Forester/    # macOS
├── bin/
│   └── forester

~/Library/Application Support/Blender/4.0/scripts/addons/  # macOS
└── difference-machine/

C:\Program Files\Forester\  # Windows
├── bin/
│   └── forester.exe

%APPDATA%\Blender Foundation\Blender\4.0\scripts\addons\  # Windows
└── difference-machine/
```

## 🔗 Полезные ссылки

- [Документация Forester](../forester/README.md)
- [Инструкция по сборке](../forester/BUILD.md)
- [Быстрый старт](../forester/QUICK_START.md)

## 📄 Лицензия

См. основной файл лицензии проекта.

## 🐛 Сообщить о проблеме

Если вы нашли ошибку или у вас есть предложения:
1. Создайте issue в репозитории проекта
2. Опишите проблему и шаги для воспроизведения
3. Укажите версию ОС и установщика

---

**Версия установщика:** 1.0.0  
**Последнее обновление:** 2024
